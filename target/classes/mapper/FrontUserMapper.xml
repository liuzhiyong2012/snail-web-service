<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.snail.web.modules.frontuser.mapper.FrontUserMapper">


    <select id="page" resultType="com.snail.web.modules.frontuser.dto.response.FrontUserReponse">
        select us.id, us.username,us.phone,us.role_type as roleType, us.created_time as createdTime
         from `front_user` us
         where 1=1
         <if test="username != null">
             and ur.username=#{username}
         </if>
        <if test="phone != null">
            and id !=#{id}
        </if>
        limit #{start},#{pageSize}
    </select>
    <select id="count" resultType="java.lang.Integer">
            select count(1) from `front_user`
            where 1=1

            <if test="phone != null">
                and phone !=#{phone}
            </if>
    </select>


</mapper>
